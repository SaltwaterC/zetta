[package]
name = "settings_content"
version = "0.1.0"
edition = "2024"
publish = false
license = "GPL-3.0-or-later"

# Resolved from `zed/Cargo.toml`'s `[workspace.lints]`, which a fork outside
# that workspace cannot inherit. Keep it in step when syncing.
[lints.rust]
unexpected_cfgs = { level = "allow" }

[lints.clippy]
dbg_macro = "deny"
todo = "deny"
declare_interior_mutable_const = "deny"
redundant_clone = "deny"
disallowed_methods = "deny"
style = { level = "allow", priority = -1 }
type_complexity = "allow"
let_underscore_future = "allow"
single_range_in_vec_init = "allow"
too_many_arguments = "allow"
large_enum_variant = "allow"
nonminimal_bool = "allow"

[lib]
path = "src/settings_content.rs"

[features]
default = []

[dependencies]
anyhow = "1.0.86"
collections = { version = "0.1.0", path = "../../zed/crates/collections" }
derive_more = { version = "2.1.1", features = ["add", "add_assign", "deref", "deref_mut", "display", "from", "from_str", "mul", "mul_assign", "not"] }
gpui = { path = "../gpui", default-features = false }
language_model_core = { path = "../../zed/crates/language_model_core" }
log = { version = "0.4.16", features = ["kv_unstable_serde", "serde"] }
schemars = { version = "1.0", features = ["indexmap2"] }
serde = { version = "1.0.221", features = ["derive", "rc"] }
serde_json = { version = "1.0.144", features = ["preserve_order", "raw_value"] }
serde_json_lenient = { version = "0.2", features = ["preserve_order", "raw_value"] }
serde_path_to_error = "0.1.17"
settings_json = { path = "../../zed/crates/settings_json", default-features = false }
settings_macros = { path = "../../zed/crates/settings_macros" }
strum = { version = "0.28", features = ["derive"] }
util = { path = "../../zed/crates/util" }
